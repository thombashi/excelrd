sudo: false
cache: pip

language: python

matrix:
  include:
    - python: "3.7"
      env: TOXENV=py37
    - python: "3.6"
      env: TOXENV=py36
    - python: "3.5"
      env: TOXENV=py35

install:
  - travis_retry pip install tox

script:
  - tox
